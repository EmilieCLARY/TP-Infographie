const materialPoints = new THREE.PointsMaterial({
    size: 0.1,
    color: 0xFF0000
})

const materialPoly = new THREE.LineBasicMaterial({ color: 0x0000FF });

const materialCasteljau= new THREE.LineBasicMaterial( { color : 0x00FF00 } );

const materialBernstein = new THREE.LineBasicMaterial( { color : 0xFF00FF } );

const materialDeBoor = new THREE.LineBasicMaterial( { color : 0x00FF00 } );
